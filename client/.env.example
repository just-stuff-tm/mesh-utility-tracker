# Cloudflare Worker URL for data ingestion
# For development with local worker
VITE_WORKER_URL=http://127.0.0.1:8787

# For production with deployed worker
# VITE_WORKER_URL=https://your-worker.your-subdomain.workers.dev
